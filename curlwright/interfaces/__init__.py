"""Interface layer: CLI and report presenters.

Consumers import the concrete entry points from the submodules
(``curlwright.interfaces.cli_app`` / ``.contracts`` / ``.sarif``) or through
the public top-level facades (``curlwright.contracts`` / ``curlwright.sarif``),
so this package intentionally re-exports nothing.
"""
